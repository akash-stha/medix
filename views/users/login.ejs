<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Hospitant || Login</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/login.css">
    <script src="/js/jquery-3.1.0.min.js"></script>
    <script src="/js/bootstrap.min.js"></script>
</head>
<body>
   <div class="container" style="margin-top:1%">
        <div class="container text-center" style="margin-top:0%">
                <% if(message && message.length > 0) { %>
                  <div class="alert alert-danger"><p><%=message %></p></div>
                <% } %>
                <% if(welcomeMsg && welcomeMsg.length > 0) { %>
                    <div class="alert alert-info" style="height:50px"><p><%=welcomeMsg %></p></div>
                <% } %>
                <% if(notLoginMsg && notLoginMsg.length > 0) { %>
                    <div class="alert alert-danger" style="height:50px"><p><%=notLoginMsg %></p></div>
                <% } %>
        </div>
        <div class="col-xs-3 col-sm-3 col-md-3 col-lg-3" style="height:100px;margin-left:44%">
          <a class="homepage" href="/"><img src="/img/1st.png" alt="Hospitant" height="100%" width="auto"></a>
        </div>
        <div class="row">
           <div class="col-xs-8 col-sm-8 col-md-8 col-lg-8 login-form">
                <h4 class="page-header">Have already account? Login here</h4>
                <form method="post" action="/login">
                    <div class="form-group">
                        <label>Email</label>
                        <input type="email" name="email" placeholder="Enter E-mail" required="" class="form-control"> 
                    </div>
                    <div class="form-group">
                        <label>Password</label>
                        <input type="password" name="password" placeholder="Password" required="" class="form-control is-invalid">
                    </div>
                    <button type="submit" class="btn btn-primary active" style="border-radius:0%">Login</button>
                </form>
                <h5>Have problems in login ?<a class="problem-link" href="/forget-password">Forget Password</a></h5>
           </div>
       </div>
       <div class="row">
          <div class="col-xs-8 col-sm-8 col-md-8 col-lg-8 register-section">
               <h4 class="page-header">Dont have Account yet, Register Now !!</h4>
          </div>
        </div>   
        <div class="register-btn">
            <ul class="list-inline list-register">
              <li class="reg-list">
                <form action="/users/register-patient">
                  <button class="btn btn-primary btn-md active reg">Register As Patient</button>
                </form>
              </li>
              <li class="reg-list">
                  <form action="/users/register-hospital">
                    <button class="btn btn-primary btn-md active reg">Register As Hospital</button>
                  </form>
              </li>
              <li class="reg-list">
                  <form action="/users/register-pharma">
                    <button class="btn btn-primary btn-md active reg">Register As Pharmacy</button>
                  </form>
              </li>
            </ul>
        </div>
        <div class="footer" style="text-align:center">
          <p class="footer-text">
              Copyright &copy 2017 || <a href="/" style="text-decoration:none">Hospitant</a>
          </p>
        </div>
    </div>
   </div>
</body>
</html>